<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.gestionstockproduit">
<uses-permission android:name="android.permssion.INTERNET"></uses-permission>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"></uses-permission>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Gestionstockproduit">
        <activity android:name=".BonDentree"></activity>
        <activity android:name=".BonDeSortie"></activity>
        <activity android:name=".CategoryListActivity"></activity>
        <activity android:name=".CategoryAjoutForm"></activity>
        <activity android:name=".ProductAjoutForm"></activity>
        <activity android:name=".ProductUpdateForm"></activity>
        <activity android:name=".ProductListActivity"></activity>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>